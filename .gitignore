.env
venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.cursor/
.venv/
uploads/
agent_checkpoints.sqlite
# eval result JSONs are committed (Option A — Railway ephemeral filesystem)
# keep only the latest 2-3 runs; delete older files before committing
tests/results/*.txt
